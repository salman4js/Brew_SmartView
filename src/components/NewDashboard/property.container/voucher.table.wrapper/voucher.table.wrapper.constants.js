var voucherTableWrapperConstants = Object.freeze({
    voucherTablePerspectiveConstants: 'voucher-table-view',
    tableInfoMessage: Object.freeze({
        ZERO_STATE_MESSAGE: 'You have no records for this table!'
    })
});

export default voucherTableWrapperConstants;